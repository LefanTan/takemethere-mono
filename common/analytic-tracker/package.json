{
  "name": "analytic-tracker",
  "version": "1.0.10",
  "description": "Tracks user analytics such as clicks, duration, pages per session, or other user defined events",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "nodemon ./src/index.ts",
    "build": "esbuild ./src/index.ts --bundle --sourcemap --format=esm --minify --outfile=./dist/index.js",
    "postbuild": "tsc --emitDeclarationOnly --declaration",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4",
    "esbuild": "^0.15.13"
  }
}
